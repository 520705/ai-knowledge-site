---
title: 全参数微调
date: 2026-04-18
tags:
  - LLM
  - Fine-tuning
  - SFT
  - Deep-Learning
  - Transformer
categories:
  - 人工智能
  - 大模型微调
alias: Full Parameter Fine-tuning
---

## 关键词

| 术语 | 英文 | 核心含义 |
|------|------|----------|
| 全参数微调 | Full Parameter Fine-tuning | 更新模型所有参数 |
| 监督微调 | Supervised Fine-tuning (SFT) | 使用标注数据训练 |
| 学习率调度 | Learning Rate Schedule | 动态调整学习率 |
| Warmup | Warmup Strategy | 预热阶段策略 |
| 梯度累积 | Gradient Accumulation | 模拟大batch训练 |
| 混合精度 | Mixed Precision Training | FP16/BF16混合计算 |
| 预训练 | Pre-training | 初始训练阶段 |
| 灾难性遗忘 | Catastrophic Forgetting | 遗忘先前知识 |
| 过拟合 | Overfitting | 训练数据过度拟合 |
| 迁移学习 | Transfer Learning | 知识迁移技术 |

---

## 概述

全参数微调（Full Parameter Fine-tuning，简称 Full FT）是深度学习领域中最传统也最直接的模型适应方法。其核心理念是通过在特定任务数据集上继续训练，更新预训练模型的所有参数，使其从通用能力向垂直领域专用能力转变。全参数微调是 LLM 适配任务的"基准方法"，后续出现的各种参数高效微调技术（PEFT）都是在全参数微调基础上的优化与权衡。

本文档将系统阐述全参数微调的原理、关键技术点、训练配置以及实际应用中的注意事项。

---

## 1. 全参数微调原理

### 1.1 核心思想

全参数微调的本质是在目标任务数据集上执行标准的监督学习流程。与预训练阶段的 [[自回归语言建模]] 不同，全参数微调通常采用 [[监督微调（Supervised Fine-tuning）]] 方式，即利用人工标注或模型生成的高质量指令-响应对数据进行训练。

```python
# 全参数微调核心训练循环
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from datasets import load_dataset

# 加载预训练模型
model = AutoModelForCausalLM.from_pretrained(
    "meta-llama/Llama-2-7b-hf",
    torch_dtype=torch.bfloat16,
    device_map="auto"
)

tokenizer = AutoTokenizer.from_pretrained("meta-llama/Llama-2-7b-hf")
tokenizer.pad_token = tokenizer.eos_token

# 准备数据集
def preprocess_function(examples):
    inputs = tokenizer(
        examples["instruction"],
        truncation=True,
        max_length=512,
        padding="max_length"
    )
    labels = tokenizer(
        examples["output"],
        truncation=True,
        max_length=512,
        padding="max_length"
    )
    inputs["labels"] = labels["input_ids"]
    return inputs

dataset = load_dataset("json", data_files="training_data.json")
tokenized_dataset = dataset.map(preprocess_function, batched=True)

# 全参数微调配置
training_args = TrainingArguments(
    output_dir="./full_ft_checkpoints",
    per_device_train_batch_size=2,
    gradient_accumulation_steps=8,  # 有效batch_size = 2 * 8 = 16
    learning_rate=2e-5,
    num_train_epochs=3,
    warmup_ratio=0.03,
    logging_steps=10,
    save_steps=500,
    save_total_limit=2,
    bf16=True,  # 启用BF16混合精度
    dataloader_num_workers=4,
    remove_unused_columns=False,
)

# 开始全参数微调
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset["train"],
    tokenizer=tokenizer,
)

trainer.train()
```

### 1.2 与预训练的区别

全参数微调虽然技术流程与预训练相似，但存在本质区别：

| 维度 | 预训练（Pre-training） | 全参数微调（Fine-tuning） |
|------|------------------------|---------------------------|
| 数据规模 | 万亿tokens级别 | 百万至千万tokens |
| 计算资源 | 超大规模GPU集群 | 单卡或少量GPU |
| 目标 | 学习通用语言能力 | 适应特定任务能力 |
| 训练稳定性 | 需要书籍、代码等多样化数据 | 任务数据质量是关键 |
| 过拟合风险 | 较低 | 较高，需正则化 |

---

## 2. SFT（监督微调）基础

### 2.1 SFT流程

[[监督微调（SFT）]] 是全参数微调最常用的训练范式。其核心思想是构建高质量的指令数据集，包含：

- **指令（Instruction）**：描述任务的自然语言描述
- **输入（Input）**：任务的具体输入（可选）
- **输出（Output）**：期望的标准答案

> [!note]
> SFT的数据质量直接决定了微调效果的上限。相比模型大小或训练技巧，高质量数据往往能带来更显著的提升。

### 2.2 训练数据格式

常见的SFT数据格式包括Alpaca格式、ShareGPT格式等：

```json
// Alpaca格式
{
    "instruction": "请将以下中文翻译成英文",
    "input": "人工智能正在改变世界",
    "output": "Artificial intelligence is changing the world."
}

// 多轮对话格式
{
    "conversations": [
        {"from": "human", "value": "什么是大语言模型？"},
        {"from": "gpt", "value": "大语言模型是..."},
        {"from": "human", "value": "它如何训练？"},
        {"from": "gpt", "value": "训练主要包括..."}
    ]
}
```

---

## 3. 学习率调度（LR Schedule）

### 3.1 学习率的重要性

学习率是深度学习最重要的超参数之一。在全参数微调中，学习率的设置比预训练更为关键，原因如下：

1. **预训练已收敛**：模型参数已接近最优解附近，需要更小的步长
2. **数据规模有限**：小数据集上大学习率极易导致过拟合
3. **任务偏移**：从通用能力到专用能力的转换需要谨慎的参数更新

> [!warning]
> 全参数微调的学习率通常设置为预训练的 **1/10 到 1/100**。例如，如果预训练学习率为1e-3，则微调学习率通常在1e-5到1e-4之间。

### 3.2 常用学习率调度器

```python
from transformers import get_cosine_schedule_with_warmup

# 余弦退火调度器配置
learning_rate = 2e-5
warmup_ratio = 0.03
num_training_steps = len(dataloader) * num_epochs

scheduler = get_cosine_schedule_with_warmup(
    optimizer,
    num_warmup_steps=int(num_training_steps * warmup_ratio),
    num_training_steps=num_training_steps,
    num_cycles=0.5,  # 半余弦衰减
)
```

### 3.3 学习率选择指南

| 模型规模 | 推荐学习率范围 | 备注 |
|----------|----------------|------|
| 7B 参数 | 1e-5 ~ 3e-5 | 7B模型通常用2e-5 |
| 13B 参数 | 8e-6 ~ 2e-5 | 更大模型需更小学习率 |
| 30B+ 参数 | 5e-6 ~ 1e-5 | 高效参数微调可能更合适 |

---

## 4. Warmup策略

### 4.1 Warmup原理

Warmup（预热）是一种防止训练早期参数剧烈波动的策略。在训练初期，学习率从零或很小的值逐渐增加到预设的学习率，帮助模型：

- 适应数据分布的统计特性
- 避免早期梯度更新方向错误
- 稳定训练过程

> [!note]
> Warmup 在大规模模型训练中尤为重要。Transformer模型的注意力机制在训练初期非常敏感，不当的学习率可能导致训练崩溃。

### 4.2 Warmup配置

```python
# Warmup配置选项

# 方案1：线性Warmup + 余弦衰减
training_args = TrainingArguments(
    warmup_ratio=0.03,  # 总步数的3%用于warmup
    lr_scheduler_type="cosine",
)

# 方案2：constant with warmup（恒定学习率warmup后保持）
training_args = TrainingArguments(
    warmup_steps=100,  # 前100步进行warmup
    lr_scheduler_type="constant",
)

# 方案3：线性warmup + 线性衰减
training_args = TrainingArguments(
    warmup_ratio=0.1,
    lr_scheduler_type="linear",
)
```

### 4.3 Warmup比例选择

| 训练规模 | 推荐Warmup比例 | 适用场景 |
|----------|----------------|----------|
| 小数据集（<10K） | 5%~10% | 快速收敛，避免过拟合 |
| 中等数据集（10K~100K） | 3%~5% | 平衡收敛速度与稳定性 |
| 大数据集（>100K） | 1%~3% | 充分探索参数空间 |

---

## 5. 梯度累积与混合精度训练

### 5.1 梯度累积

当GPU显存不足以容纳大batch训练时，梯度累积通过将多个小batch的梯度累加，模拟大batch的更新效果：

```python
# 梯度累积原理
effective_batch_size = per_device_batch_size * gradient_accumulation_steps

# 每gradient_accumulation_steps步更新一次参数
for step, batch in enumerate(dataloader):
    loss = model(batch).loss
    loss = loss / gradient_accumulation_steps  # 归一化损失
    loss.backward()
    
    if (step + 1) % gradient_accumulation_steps == 0:
        optimizer.step()
        optimizer.zero_grad()
```

### 5.2 混合精度训练

[[混合精度训练（Mixed Precision Training）]] 通过同时使用FP16/BF16和FP32来加速训练并减少显存占用：

```python
training_args = TrainingArguments(
    bf16=True,  # BF16混合精度（推荐用于Ampere架构+）
    # fp16=True,  # 或使用FP16
    fp16_backend="apex",  # 可选：使用Apex实现
)
```

> [!tip]
> **BF16 vs FP16**：
> - BF16：更大的指数范围，不易溢出，推荐用于 [[LLaMA]]、ChatGLM等现代模型
> - FP16：更省显存，但需要更仔细的损失缩放（loss scaling）

### 5.3 显存占用对比

| 精度格式 | 7B模型显存 | 13B模型显存 | 效果 |
|----------|------------|-------------|------|
| FP32 | ~28GB | ~52GB | 基准 |
| FP16/BF16 | ~14GB | ~26GB | 2x压缩 |
| BF16 + 梯度检查点 | ~10GB | ~18GB | 3x压缩 |
| 优化器状态FP32 | +12GB | +22GB | 额外开销 |

---

## 6. 典型训练配置

### 6.1 7B模型全参数微调配置

```yaml
# deepspeed_config.json (ZeRO Stage 2)
{
    "zero_optimization": {
        "stage": 2,
        "offload_optimizer": {
            "device": "cpu",
            "pin_memory": true
        },
        "allgather_partitions": true,
        "allgather_bucket_size": 2e8,
        "overlap_comm": true,
        "reduce_scatter": true,
        "reduce_bucket_size": 2e8,
        "contiguous_gradients": true
    },
    "fp16": {
        "enabled": "auto",
        "loss_scale": 0,
        "loss_scale_window": 1000,
        "initial_scale_power": 16,
        "hysteresis": 2,
        "min_loss_scale": 1
    },
    "gradient_accumulation_steps": "auto",
    "gradient_clipping": "auto",
    "steps_per_print": 10,
    "train_batch_size": "auto",
    "train_micro_batch_size_per_gpu": "auto",
    "wall_clock_breakdown": false
}
```

### 6.2 完整训练脚本

```python
#!/usr/bin/env python3
"""
全参数微调完整训练脚本
适用于7B-13B规模模型
"""

import torch
from transformers import (
    AutoModelForCausalLM,
    AutoTokenizer,
    TrainingArguments,
    Trainer,
    DataCollatorForLanguageModeling,
    EarlyStoppingCallback,
)
from datasets import load_dataset

def setup_model_and_tokenizer(model_name, load_in_8bit=False):
    """初始化模型和分词器"""
    tokenizer = AutoTokenizer.from_pretrained(
        model_name,
        trust_remote_code=True,
        use_fast=False,
    )
    tokenizer.pad_token = tokenizer.eos_token
    
    if load_in_8bit:
        from transformers import BitsAndBytesConfig
        quantization_config = BitsAndBytesConfig(
            load_in_8bit=True,
            llm_int8_threshold=6.0,
            llm_int8_has_fp16_weight=False,
        )
    else:
        quantization_config = None
    
    model = AutoModelForCausalLM.from_pretrained(
        model_name,
        quantization_config=quantization_config,
        torch_dtype=torch.bfloat16 if torch.cuda.is_available() else torch.float32,
        device_map="auto" if load_in_8bit else None,
        trust_remote_code=True,
    )
    
    return model, tokenizer

def tokenize_function(examples, tokenizer, max_length=2048):
    """数据预处理"""
    # 合并instruction和input作为输入
    inputs = []
    for instr, inp in zip(examples["instruction"], examples["input"]):
        if inp:
            text = f"指令: {instr}\n输入: {inp}\n回答: "
        else:
            text = f"指令: {instr}\n回答: "
        inputs.append(text)
    
    # Tokenize
    model_inputs = tokenizer(
        inputs,
        max_length=max_length,
        truncation=True,
        padding="max_length",
        return_tensors=None,
    )
    
    # 添加标签（模型输入部分不需要计算loss）
    labels = model_inputs["input_ids"].copy()
    # instruction和input部分设为-100，在计算loss时忽略
    instr_len = len(tokenizer(f"指令: {examples['instruction'][0]}\n回答: ", return_tensors=None)["input_ids"])
    
    model_inputs["labels"] = labels
    return model_inputs

def main():
    # 配置
    MODEL_NAME = "meta-llama/Llama-2-7b-hf"
    OUTPUT_DIR = "./full_ft_output"
    MAX_LENGTH = 2048
    BATCH_SIZE = 4
    GRADIENT_ACCUMULATION = 4
    EPOCHS = 3
    LEARNING_RATE = 2e-5
    WARMUP_RATIO = 0.03
    
    # 初始化
    model, tokenizer = setup_model_and_tokenizer(MODEL_NAME, load_in_8bit=True)
    
    # 加载数据
    dataset = load_dataset("json", data_files="train.json")["train"]
    tokenized_dataset = dataset.map(
        lambda x: tokenize_function(x, tokenizer, MAX_LENGTH),
        batched=True,
        remove_columns=dataset.column_names,
    )
    
    # 训练参数
    training_args = TrainingArguments(
        output_dir=OUTPUT_DIR,
        per_device_train_batch_size=BATCH_SIZE,
        gradient_accumulation_steps=GRADIENT_ACCUMULATION,
        learning_rate=LEARNING_RATE,
        num_train_epochs=EPOCHS,
        warmup_ratio=WARMUP_RATIO,
        lr_scheduler_type="cosine",
        logging_dir=f"{OUTPUT_DIR}/logs",
        logging_steps=10,
        save_steps=500,
        save_total_limit=2,
        bf16=True,
        dataloader_num_workers=4,
        remove_unused_columns=False,
        report_to=["tensorboard"],
        load_best_model_at_end=True,
        metric_for_best_model="eval_loss",
        greater_is_better=False,
    )
    
    # Trainer
    trainer = Trainer(
        model=model,
        args=training_args,
        train_dataset=tokenized_dataset,
        tokenizer=tokenizer,
        data_collator=DataCollatorForLanguageModeling(tokenizer, mlm=False),
        callbacks=[EarlyStoppingCallback(early_stopping_patience=3)],
    )
    
    # 开始训练
    trainer.train()
    
    # 保存最终模型
    trainer.save_model(f"{OUTPUT_DIR}/final_model")
    trainer.save_state()

if __name__ == "__main__":
    main()
```

---

## 7. 全参数微调的优缺点

### 7.1 优势

| 优势 | 说明 |
|------|------|
| 效果最佳 | 在数据充足的情况下，通常能获得最佳任务性能 |
| 灵活性高 | 可完全自定义模型行为，不受参数更新限制 |
| 适用性广 | 适用于所有模型架构和任务类型 |
| 调试直观 | 所有参数都可检查和可视化 |

### 7.2 局限性

| 局限 | 说明 |
|------|------|
| 计算成本高 | 需要多卡GPU，大模型训练成本昂贵 |
| 存储开销大 | 每个任务需要保存完整模型副本 |
| 训练时间长 | 完整遍历所有参数，迭代速度慢 |
| 灾难性遗忘 | 可能遗忘预训练学到的通用能力 |

> [!caution]
> 全参数微调的主要风险是 **[[灾难性遗忘（Catastrophic Forgetting）]]**。当目标任务数据分布与预训练数据差异较大时，模型可能丧失原有的通用能力。建议在微调时混入预训练数据或使用 [[RLHF]] 等技术缓解。

---

## 8. 实践建议

1. **数据优先**：投资高质量标注数据往往比调参更有效
2. **从小到大**：先用小模型验证流程，再扩展到大模型
3. **监控指标**：同时监控训练损失、验证损失和任务指标
4. **早停机制**：设置 early stopping 防止过拟合
5. **梯度裁剪**：设置 `max_grad_norm=1.0` 防止梯度爆炸
6. **定期保存**：使用 `save_steps` 定期保存检查点

---

## 相关文档

- [[LoRA微调深度指南]] - 参数高效微调方法
- [[QLoRA微调详解]] - 量化+Lora微调技术
- [[Adapter微调]] - Adapter机制微调
- [[P-Tuning微调]] - Prompt Tuning方法
- [[prefix微调]] - Prefix Tuning方法
- [[微调技术对比总结]] - 各技术对比分析

---

*本文档由 AI 知识库自动生成*
